# Configuration

Version 1.x uses sensible defaults and requires minimal configuration.

Future versions will support configuration files for:

- editor preferences
- terminal preferences
- AI services
- Docker
- Git
- project templates
- plugin settings
